on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: make all
        run: make all
      - name: hax
        run: |
          yes | xe -a -s "sed q" -- 1 2 3
      - name: make check
        run: make check

